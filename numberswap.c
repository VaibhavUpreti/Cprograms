#include <stdio.h>
#include <math.h>
int main()
{
  int a,b ;
  printf(" Enter the two numbers a and b:");
  scanf("%d %d", &a, &b );
  a=a+b
  b=a-b
  a=a-b
  printf("Numbers after swapping are: a=%d, b=%d", a, b );
  return 0; 
}
